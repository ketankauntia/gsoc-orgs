import { NextResponse } from "next/server";
import { createAdminClient } from "@/lib/supabase/admin";
import { projectV1 } from "@/lib/supabase/legacy-shapes";
export async function GET(_request: Request, { params }: { params: Promise<{ id: string }> }) { try { const { id } = await params; const { data, error } = await createAdminClient().from("projects").select("*,organizations(slug,name),project_contributors(archived_name,ordinal),project_mentors(name,ordinal)").eq("external_id", id).maybeSingle(); if (error) throw error; if (!data) return NextResponse.json({ success: false, error: { message: "Project not found", code: "NOT_FOUND" } }, { status: 404 }); return NextResponse.json({ success: true, data: projectV1(data), meta: { timestamp: new Date().toISOString(), version: "v1" } }, { headers: { "Cache-Control": "public, s-maxage=3600, stale-while-revalidate=86400" } }); } catch (error) { console.error("Project detail API error:", error); return NextResponse.json({ success: false, error: { message: "Failed to fetch project", code: "FETCH_ERROR" } }, { status: 500 }); } }

